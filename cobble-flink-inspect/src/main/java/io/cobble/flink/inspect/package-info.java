/**
 * Read-only Java API for discovering and inspecting Cobble Flink checkpoints, savepoints, and
 * datasource snapshots.
 *
 * <p>Open sessions are pinned to one concrete snapshot and must be closed after use.
 */
package io.cobble.flink.inspect;
